CREATE TABLE IF NOT EXISTS Funcionario (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    nome VARCHAR (50),
    idade INTEGER,
    endereco VARCHAR (100)
);

INSERT INTO Tabela
    (atributo1, atributo2)
